{
  "name": "davidalsh-com",
  "type": "module",
  "scripts": {
    "build": "node --import tsx ./scripts/index.ts",
    "watch": "npx nodemon",
    "serve": "npx http-server -c=-1 ./dist"
  },
  "dependencies": {
    "preact": "*"
  },
  "devDependencies": {
    "@types/node": "*",
    "@types/ejs": "*",
    "glob": "*",
    "tsx": "*",
    "ejs": "*",
    "esbuild": "*",
    "http-server": "*",
    "nodemon": "*",
    "sass": "*",
    "prettier": "*",
    "marked": "*",
    "prismjs": "*",
    "@types/prismjs": "*",
    "moment": "*"
  },
  "nodemonConfig": {
    "ignore": ["**/node_modules/**", "**/.git/**", "**/dist/**"],
    "watch": ["src/**/*", "src/.partials/**/*", "src/.utils/**/*", "scripts/**/*"],
    "ext": "*",
    "delay": 250,
    "signal" : "SIGTERM",
    "exec": "clear && npm run build"
  }
}
